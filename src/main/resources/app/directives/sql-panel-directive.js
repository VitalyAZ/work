ngSQLManager.directive('sqlPanelDirective', function() {
                return {
                        link: function (scope, element, attrs) {
                                scope.title = "Common";
                            });
                        }
                    }
});